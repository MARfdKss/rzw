<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">

    </style>

    <style name="FullScreenTheme" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="SplashTheme" parent="AppTheme">

    </style>

    <style name="FullScreenLikeAppTheme" parent="AppTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 有无边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 透明度 -->
        <item name="android:backgroundDimAmount">0.3</item>
        <!-- 是否透明 -->
        <item name="android:windowIsTranslucent">true</item>

        <item name="android:windowFullscreen">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 动画 -->
        <item name="android:windowAnimationStyle">@style/dialog_animation</item>

        <item name="android:backgroundDimEnabled">true</item>

    </style>


    <style name="ActivityAnimation" mce_bogus="1" parent="android:Animation">
        <item name="@android:activityOpenEnterAnimation">@anim/activity_open_enter</item>
        <item name="@android:activityOpenExitAnimation">@anim/activity_open_exit</item>
        <item name="@android:activityCloseEnterAnimation">@anim/activity_close_enter</item>
        <item name="@android:activityCloseExitAnimation">@anim/activity_close_exit</item>
    </style>

    <style name="NoneAnimation" mce_bogus="1" parent="android:Animation">

    </style>

    <style name="dialog_animation" parent="android:Animation">
        <item name="@android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="@android:windowExitAnimation">@anim/dialog_exit</item>
    </style>

    <style name="dialog_activity_animation" parent="android:Animation">
        <item name="@android:activityOpenEnterAnimation">@anim/dialog_enter</item>
    </style>


    <style name="AlertDialogStyle">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 有无边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 透明度 -->
        <item name="android:backgroundDimAmount">0.3</item>
        <!-- 是否透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 文本超出宽度时，是否出现横拉条 -->
        <item name="android:scrollHorizontally">false</item>
        <!-- 是否全屏 -->
        <item name="android:windowFullscreen">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 动画 -->
        <item name="android:windowAnimationStyle">@style/dialog_animation</item>

    </style>

    <style name="popupwindow_white_dim">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 有无边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">false</item>
        <!-- 透明度 -->
        <item name="android:backgroundDimAmount">0.7</item>
        <!-- 是否透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 文本超出宽度时，是否出现横拉条 -->
        <item name="android:scrollHorizontally">false</item>
        <!-- 是否全屏 -->
        <item name="android:windowFullscreen">false</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowCloseOnTouchOutside">true</item>
        <!-- 动画 -->
        <item name="android:windowAnimationStyle">@style/dialog_animation</item>

    </style>
</resources>
